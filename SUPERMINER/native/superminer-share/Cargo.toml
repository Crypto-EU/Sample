[package]
name = "superminer-share"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"

[lib]
crate-type = ["cdylib"]

[dependencies]
pearl-blake3 = { path = "../pearl-blake3", features = ["serde"] }
blake3 = "1.8"
